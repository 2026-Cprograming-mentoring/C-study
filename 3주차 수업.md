# 📘 C 언어 멘토링 2주차 - 함수, 포인터, 배열, 동적할당

---

## 1. 함수 (Function)

함수는 **특정 작업을 수행하는 코드 블록**이다.  
같은 코드를 반복 작성하지 않고 이름을 붙여 재사용할 수 있다.


### 1.1 함수의 구조

```c
반환타입 함수이름(매개변수) {
    // 실행할 코드
    return 반환값;
}
```

예시:

```c
int add(int a, int b) {
    return a + b;
}

int main() {
    int result = add(3, 5);
    printf("%d\n", result);  // 8
    return 0;
}
```

| 구성 요소 | 의미 |
|-----------|------|
| `int` (앞) | 반환 타입 — 함수가 돌려주는 값의 타입 |
| `add` | 함수 이름 |
| `int a, int b` | 매개변수 — 함수가 받는 입력값 |
| `return a + b` | 반환값 — 호출한 곳으로 돌려주는 값 |

---

### 1.2 매개변수와 인자

- **매개변수(parameter)**: 함수 정의에서 선언하는 변수 (`int a, int b`)
- **인자(argument)**: 함수를 호출할 때 실제로 전달하는 값 (`3, 5`)

```c
int add(int a, int b) { ... }  // a, b는 매개변수
add(3, 5);                     // 3, 5는 인자
```

---

### 1.3 반환값과 void

반환할 값이 없을 때는 반환 타입을 `void`로 지정한다.

```c
void print_hello() {
    printf("Hello!\n");
    // return 불필요
}

int main() {
    print_hello();  // Hello!
    return 0;
}
```

---

### 1.4 함수와 스택(Stack) 동작

함수가 호출될 때, 메모리의 **스택(stack)** 영역에 해당 함수의 공간(스택 프레임)이 생성된다.  
함수가 종료되면 그 공간은 **자동으로 사라진다**.

```
main() 호출
┌─────────────┐
│  main 프레임  │  ← result, x, y 등 main의 지역변수
│             │
│  add 프레임  │  ← a=3, b=5 (복사된 값)
└─────────────┘
   add() 종료 → add 프레임 사라짐
```

> **핵심** — 함수에 값을 넘기면 **복사본**이 만들어진다.  
> 함수 안에서 매개변수를 바꿔도 원본에는 영향이 없다.

```c
void change(int x) {
    x = 100;  // 복사본만 바뀜
}

int main() {
    int a = 10;
    change(a);
    printf("%d\n", a);  // 10 — 원본 그대로
    return 0;
}
```

이 한계를 극복하기 위해 **포인터**를 사용한다 (4.3 참고).

---

### 1.5 함수 선언(프로토타입)

함수를 `main()` 아래에 정의하면 컴파일러가 인식하지 못한다.  
이를 해결하기 위해 **상단에 선언(프로토타입)** 을 미리 적는다.

```c
int add(int a, int b);  // 선언 (세미콜론 주의)

int main() {
    printf("%d\n", add(3, 5));  // 문제 없음
    return 0;
}

int add(int a, int b) {  // 정의
    return a + b;
}
```

---

## 2. 메모리와 주소

프로그램에서 모든 변수는 메모리 공간에 저장된다.  
각 변수는 고유한 주소(address)를 가진다.

```c
#include <stdio.h>

int main() {
    int a = 10;
    printf("%p\n", &a); // a의 주소 출력
    return 0;
}
```

- a → 변수 a에 저장된 값  
- &a → 변수 a가 저장되어있는 주소
> **참고** — 주소 값은 실행 환경마다 다르게 출력된다. 중요한 것은 '어디에 있는가'라는 위치 개념이다.

---

## 3. 포인터 (Pointer)

포인터는 **다른 변수의 주소를 저장하는 변수**이다.  
`*`를 붙여 선언하며, 타입은 가리킬 변수의 타입과 맞춰야 한다.

```c
int  *p;   // int형 변수의 주소를 저장하는 포인터
char *c;   // char형 변수의 주소를 저장하는 포인터
```

---

### 3.1 포인터 초기화

```c
int a = 10;
int *p = &a;  // p에 a의 주소를 저장
```

| 표현 | 의미 |
|------|------|
| `p` | a의 주소 (예: 0x1A3F) |
| `*p` | p가 가리키는 곳의 값 → a의 값(10) |

### 3.2 역참조 (Dereference)

`*` 연산자를 포인터 앞에 붙이면 그 주소에 저장된 값에 접근할 수 있다.  
값을 읽는 것은 물론, 직접 수정도 가능하다.

```c
int a = 10;
int *p = &a;

printf("%d\n", *p);  // 10 — a의 값을 읽음

*p = 20;             // a의 값을 20으로 변경
printf("%d\n", a);   // 20 — 원본 변수가 바뀜
```

> **비유** — `p`는 우체통 번호, `*p`는 그 우체통 안에 들어 있는 편지다.  
> 번호를 알면(p) 편지를 꺼내거나(*p) 새 편지로 교체(*p = 새값)할 수 있다.

---

## 4. 포인터 활용 — swap 함수

함수 안에서 두 변수를 교환하려면 **값이 아니라 주소를 넘겨야** 원본이 바뀐다.

### 4.1 잘못된 예 (값을 넘기는 경우)

```c
void swap_wrong(int a, int b) {
    int temp = a;
    a = b;
    b = temp;
    // 지역 변수만 바뀌고, 호출한 쪽의 x, y는 그대로
}

swap_wrong(x, y);  // x, y 변화 없음
```

### 4.2 올바른 예 (주소를 넘기는 경우)

```c
void swap(int *a, int *b) {
    int temp = *a;  // a가 가리키는 값을 임시 저장
    *a = *b;        // b의 값을 a 자리에 씀
    *b = temp;      // 임시 저장값을 b 자리에 씀
}

int x = 1, y = 2;
swap(&x, &y);       // 주소를 넘김

printf("%d %d\n", x, y);  // 2 1 — 원본이 바뀜
```

> 이 패턴은 `scanf`에서 `&`를 쓰는 이유와 동일하다.  
> `scanf("%d", &n)` — 함수 안에서 n의 값을 직접 채워 넣기 위해 주소를 넘긴다.

---

## 5. 배열 (Array)

```c
int arr[3] = {1, 2, 3};
```

배열은 연속된 메모리 공간이다.

---

### 5.1 배열과 주소

```c
printf("%p\n", arr);
printf("%p\n", &arr[0]);
```

→ 결과 동일

---

### 5.2 배열과 포인터 관계

```c
arr[i] == *(arr + i)
```

예:

```c
printf("%d\n", arr[1]);
printf("%d\n", *(arr + 1));
```

---

### 5.3 포인터로 배열 다루기

```c
int *p = arr;

printf("%d\n", p[1]);
printf("%d\n", *(p + 1));
```

---

## 6. 정적 배열의 한계

```c
int arr[10];
```

- 크기 고정  
- 실행 전에 결정  

---

## 7. 동적할당 (Dynamic Allocation)

실행 중에 메모리를 할당하는 방법

---

### 7.1 malloc

```c
#include <stdlib.h>

int *p = (int*)malloc(sizeof(int) * 3);
```

- malloc → 메모리 할당  
- 반환값 → 주소  

---

### 7.2 배열처럼 사용

```c
p[0] = 10;
p[1] = 20;
```

---

## 8. free (메모리 해제)

```c
free(p);
```

- 메모리 누수 방지

---

## 9. 전체 예제

```c
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {
    int n;
    printf("개수 입력: ");
    scanf("%d", &n);

    int *arr = (int*)malloc(sizeof(int) * n);

    for(int i = 0; i < n; i++){
        arr[i] = i + 1;
    }

    for(int i = 0; i < n; i++){
        printf("%d ", arr[i]);
    }

    free(arr);
    return 0;
}
```

---

## 10. 핵심 정리

- 함수: 재사용 가능한 코드 블록, 호출 시 스택에 프레임 생성  
- 포인터: 주소를 저장하는 변수  
- 배열: 연속된 메모리 공간  
- 동적할당: 실행 중 메모리 생성  
- free: 메모리 해제  

---

## 11. 한줄 정리

함수는 코드를 묶어 재사용하는 단위이며,  
포인터를 함수에 넘기면 원본 데이터를 직접 조작할 수 있다.  
배열은 포인터를 기반으로 동작하며,  
동적할당은 그 포인터를 통해 메모리를 직접 사용하는 방법이다.

---

## 12. 체크 질문

- 함수의 매개변수와 인자의 차이는?
- 함수 안에서 값을 바꿔도 원본이 안 바뀌는 이유는?
- void 함수와 int 함수의 차이는?
- p와 *p의 차이는?
- arr과 &arr[0]이 같은 이유는?
- malloc의 반환값은?
- free를 하지 않으면?
